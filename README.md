# Testimonial API
#Prerequisites : 
                  -node.js
                  -MongoDb
#Steps:   
        -npm install
        -mongodb://localhost:27017/ make sure it is on same port 
        -npm start
